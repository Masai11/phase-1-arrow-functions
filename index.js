const a = 2000
const b = 100
const divide = function(){
    return a / b;
}

const square = (parameter1) => parameter1 * parameter1;
square(2,3); 

const add = (para1, para2) => para1 + para2;
add(4,6); 